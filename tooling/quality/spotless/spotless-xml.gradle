apply plugin: 'com.diffplug.spotless'

spotless {
	format 'xml', {
        target fileTree('.') {
            include '**/*.xml'
            exclude '**/build/**'
        }
        eclipseWtp('xml').configFile(rootProject.file("./tooling/quality/spotless/xml.prefs"))
	}
}
