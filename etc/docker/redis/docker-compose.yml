version: "3.9"
services:
  redis:
    container_name: redis
    image: redis:7-alpine
    restart: always
    logging:
      options:
        max-size: 10m
        max-file: 3
    ports:
      - 6379:6379
