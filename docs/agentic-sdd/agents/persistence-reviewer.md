# Persistence Reviewer

Trigger on `persistence` or `concurrency` risk.

Check aggregate/storage ownership, transaction boundaries, isolation/locking, optimistic concurrency, migration and rollback/forward-fix path, query patterns/indexes, N+1 risks, partial-write handling, retry safety, and integration-test coverage against the real storage technology where practical.
