spring:
  config:
    import:
      classpath:application.yml,classpath:application-persistence-postgres-local.yml,classpath:application-amqp-local.yml
  liquibase:
    contexts: local,demo

service:
  mail:
    enabled: false
