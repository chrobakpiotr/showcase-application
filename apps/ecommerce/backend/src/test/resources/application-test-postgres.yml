spring:
  config:
    import:
      classpath:application.yml,classpath:application-persistence-postgres.yml
  liquibase:
    contexts: local,demo

management:
  health:
    rabbit:
      enabled: false

service:
  rabbitmq:
    enabled: false
  mail:
    enabled: false
